# tools/

Home for developer-facing tooling and one-off utilities (code generators, fixture builders,
protocol-sync helpers, evaluation harnesses) that are not part of the shipped engine or app.

Empty for now — add tools here as needs arise rather than bloating `scripts/` or the engine.
